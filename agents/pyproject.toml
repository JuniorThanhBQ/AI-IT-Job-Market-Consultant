[project]
name = "agents"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "crawlee[all]>=1.8.2",
    "asyncpg>=0.30.0",
    "backend",
    "celery>=5.4.0",
    "langchain>=1.3.14",
    "aiohttp>=3.14.3",
    "crawl4ai>=0.9.2",
    "docling>=2.115.0",
]

[tool.uv.sources]
backend = { workspace = true }

[tool.pytest.ini_options]
pythonpath = ["..", "."]

[tool.setuptools]
packages = ["subagents", "supervisor", "prompt_templates"]
