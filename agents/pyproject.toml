[project]
name = "agents"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "crawlee[all]>=1.8.2",
    "asyncpg>=0.30.0",
    "backend",
    "celery>=5.4.0",
    "langchain>=1.3.14",
]

[tool.uv.sources]
backend = { workspace = true }
