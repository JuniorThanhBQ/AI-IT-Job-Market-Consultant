[project]
name = "ai-it-job-market-consultant"
version = "0.1.0"
description = "Welcome to my graduate project"
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[dependency-groups]
dev = [
    "pre-commit>=4.6.1",
]

[tool.uv.workspace]
members = [
    "backend",
]
