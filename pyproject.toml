[project]
name = "ai-it-job-market-consultant"
version = "0.1.0"
description = "This project used uv to manage python package"
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[dependency-groups]
dev = [
    "pre-commit>=4.6.0",
]

[tool.uv.workspace]
members = [
    "backend",
    "agents",
]

[tool.pytest.ini_options]
pythonpath = ["."]
